/* ============================================================================
   Oryxen Landing Page — production configuration TEMPLATE
   ----------------------------------------------------------------------------
   Before deploying (Firebase Hosting), copy this over `config.js` and set
   `webAppUrl` to the REAL public URL of the Oryxen Web Application. Every
   [data-app-link] CTA (Open the app / Get Started) resolves against this base,
   so leaving localhost here would break all CTAs in production.
   ========================================================================== */
window.ORYXEN_CONFIG = {
  // Public URL of the deployed Oryxen Web Application (Vercel).
  webAppUrl: 'https://oryxen-web-application.vercel.app',
};
